import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.coink.app',
  appName: 'coink app',
  webDir: '../dist/coink-app/browser',
};

export default config;
